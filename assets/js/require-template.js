// Fichier Definition de Module RequireJs
define(['jquery'], function($) {

    var initialize = function() {
        // Fonctions a executer une fois le module chargé & initialize() appelé.
    };

    return {
        initialize: initialize
        // Renvoi la fonction initialize
        // Pour l'utiliser dans d'autres modules
    };
});